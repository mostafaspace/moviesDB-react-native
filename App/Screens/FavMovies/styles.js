import {StyleSheet} from 'react-native';

export default StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
    },
    errorContainer: {
        marginTop: 32,
        margin: 16,
    },
    errorMsg: {
        fontSize: 14,
        color: 'red',
        textAlign: 'center',
    },
});
