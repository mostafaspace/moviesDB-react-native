import {StyleSheet} from 'react-native';

export default StyleSheet.create({
    container: {
        flex: 1,
    },
    flatContainerStyle: {
        paddingTop: 16,
        paddingHorizontal: 8,
    },
});
