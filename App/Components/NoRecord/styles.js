import {StyleSheet} from 'react-native';

export default StyleSheet.create({
    emptyListContainer: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
    },
    emptyListText: {
        lineHeight: 18,
        color: '#222',
        textAlign: 'center',
        margin: 16,
    },
});
